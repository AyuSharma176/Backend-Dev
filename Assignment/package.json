{
  "name": "assignment-middleware-mongoose",
  "version": "1.0.0",
  "description": "Backend assignment with Express middleware and Mongoose middleware plugins",
  "main": "app.js",
  "type": "commonjs",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "check": "Get-ChildItem -Recurse -Filter *.js | ForEach-Object { node --check $_.FullName }"
  },
  "keywords": [
    "express",
    "mongoose",
    "middleware",
    "assignment"
  ],
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.4.1"
  }
}
